/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['img.freepik.com', 'i.pinimg.com', 'cdn.sanity.io'], // Add any additional domains as needed
  },
};

module.exports = nextConfig;
